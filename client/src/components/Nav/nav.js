import React from 'react';
import { Link } from 'react-router-dom';
import Styled from 'styled-components';

const Nav = (props) => {
    return (
        <div>
            
        </div>
    );
};

export default Nav;
