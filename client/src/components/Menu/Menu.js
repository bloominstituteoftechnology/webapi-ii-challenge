import React from 'react';

import './Menu.css';

export default () => {
    return (
        <nav className="Menu">
            <button className="Menu__button">Create</button>
        </nav>
    )
}